print("Hồ Đình Huân")
print("MSSV:235752021610049")
import tkinter as tk

# Hàm xử lý khi bấm nút "Click Me"
def show_choice():
    # Lấy lựa chọn của radio button và hiển thị
    choice = v.get()
    result_label.config(text=f"Lựa chọn của bạn là: {choice}")

# Khởi tạo cửa sổ giao diện
root = tk.Tk()
root.title("Thông Tin Cá Nhân")

# Tạo các label và entry cho thông tin cá nhân
tk.Label(root, text="Họ và tên:").grid(row=0, column=0, padx=10, pady=5)
name_entry = tk.Entry(root)
name_entry.grid(row=0, column=1, padx=10, pady=5)

tk.Label(root, text="Ngày sinh (dd/mm/yyyy):").grid(row=1, column=0, padx=10, pady=5)
dob_entry = tk.Entry(root)
dob_entry.grid(row=1, column=1, padx=10, pady=5)

tk.Label(root, text="MSSV:").grid(row=2, column=0, padx=10, pady=5)
mssv_entry = tk.Entry(root)
mssv_entry.grid(row=2, column=1, padx=10, pady=5)

tk.Label(root, text="Ngành học:").grid(row=3, column=0, padx=10, pady=5)
major_entry = tk.Entry(root)
major_entry.grid(row=3, column=1, padx=10, pady=5)

# Thêm radio buttons
tk.Label(root, text="Chọn một lựa chọn:").grid(row=4, column=0, columnspan=2, padx=10, pady=10)
v = tk.IntVar()
v.set(1)  # Thiết lập lựa chọn mặc định là 1

radio1 = tk.Radiobutton(root, text="Lựa chọn 1", variable=v, value=1)
radio1.grid(row=5, column=0, padx=10, pady=5)

radio2 = tk.Radiobutton(root, text="Lựa chọn 2", variable=v, value=2)
radio2.grid(row=5, column=1, padx=10, pady=5)

radio3 = tk.Radiobutton(root, text="Lựa chọn 3", variable=v, value=3)
radio3.grid(row=6, column=0, columnspan=2, padx=10, pady=5)

# Thêm nút "Click Me" để hiển thị lựa chọn
click_button = tk.Button(root, text="Click Me", command=show_choice)
click_button.grid(row=7, column=0, columnspan=2, padx=10, pady=10)

# Label hiển thị lựa chọn sau khi bấm nút "Click Me"
result_label = tk.Label(root, text="Lựa chọn của bạn sẽ hiển thị ở đây")
result_label.grid(row=8, column=0, columnspan=2, padx=10, pady=5)

# Khởi động ứng dụng GUI
root.mainloop()
